# Admin Authentication
# Generate a hash using: node scripts/hash-password.js <your-password>
# Important: Wrap the hash in single quotes to prevent $ character issues
ADMIN_PASSWORD_HASH=''

# Session Security (generate a random secret for production)
SESSION_SECRET=

# GitHub Integration (for content commits)
GITHUB_TOKEN=
GITHUB_REPO=
GITHUB_BRANCH=main
